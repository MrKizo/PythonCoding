# Source code is orginal code you write on ur pc 
# Transalation is converting ur soruce code in machine language 
# Compilation is translationg the code before the intial run time
# Runtime is the period the app takes to excute the code/commands
# Interpretation means that the code gets excuted immediately 

# Escape sequences characters: \b = backspace (deletes the previous character)

# Escape new line + \ = simply allows you to divide the code into seperate lines

# Escape backslash: to add a backslash to the terminal simply add another "\" so like this: print("i love kimo \\") and the backslash will appear in the terminal 

# Escape backslash single quote 

# print ('I love Kimo 1 \'test\'')

# double quote

# print ("I love Kimo too \"test\"")

# line feed to sperate the code onto 2 seprate lines (\n)

# carriarge returen (/r) repalces the code itself what follows /r with the characters behind it 

# Horizental tab (\t) creates a tab between the variables (simply makes it like you pressed space between em)

# Character hex value (\xhh)  

# Concatenation is connecting strings with each other you can either add space in the variables itself orrrr in the command itslef by print (msg + " " + lang)